[pytest]
timeout = 60
asyncio_mode = auto
testpaths = tests
markers =
    integration: Integration tests
    slow: Slow tests
    concept: Verification for a CONCEPT:* traceability id, e.g. concept("ROM-001")
addopts = -m "not integration"
filterwarnings =
    ignore:.*exclude_args.*
